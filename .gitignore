.venv
__pycache__
*.pyc
*.egg-info
.zig-cache
zig-out
/block_store
/chunk_store
.pytest_cache
.vscode/
.coverage*
/build/
/openfhe_test

# OpenHands/AllHands cache
/tmp/dcypher_cache

# TUI Screenshots and generated images
/screenshots/

# Crypto files from old approach (should not be in repo root)
cc.json
key.pub
key.sec
*.pub
*.sec
rekey.json
ciphertext.idk
idk_signing.pub
idk_signing.sec
liboqs-local/
openfhe-local/
target/

# Static library installs (built via `just build-deps`)
vendor/openfhe-install/
vendor/liboqs-install/
# HumanLayer thoughts (local only)
thoughts/

# Buf generated code (use buf.build registry instead)
gen/
.omc

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# cargo-fuzz generated state (seeds regenerate via `just fuzz-corpus`)
crates/recrypt-ffi/fuzz/corpus/
crates/recrypt-ffi/fuzz/artifacts/
crates/recrypt-ffi/fuzz/coverage/
crates/recrypt-ffi/fuzz/target/
