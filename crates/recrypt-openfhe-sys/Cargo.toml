[package]
name = "recrypt-openfhe-sys"
version = "0.1.0"
edition = "2024"
description = "Minimal OpenFHE bindings for Proxy Recryption (PRE) operations"
license.workspace = true

[dependencies]
cxx = "1.0"

[build-dependencies]
cxx-build = "1.0"
