// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from "./client";
import { client } from "./client.gen";
import type {
  CreateAccountData,
  CreateAccountErrors,
  CreateAccountResponses,
  VerifyCapabilityData,
  VerifyCapabilityErrors,
  VerifyCapabilityResponses,
} from "./types.gen";

export type Options<
  TData extends TDataShape = TDataShape,
  ThrowOnError extends boolean = boolean,
  TResponse = unknown,
> = Options2<TData, ThrowOnError, TResponse> & {
  /**
   * You can provide a client instance returned by `createClient()` instead of
   * individual options. This might be also useful if you want to implement a
   * custom client.
   */
  client?: Client;
  /**
   * You can pass arbitrary values through the `meta` object. This can be
   * used to access values that aren't defined as part of the SDK function.
   */
  meta?: Record<string, unknown>;
};

/**
 * Create a new account.
 *
 * Registers the caller's dual-stack public keys (Ed25519 + ML-DSA-87)
 * under the BLAKE3 fingerprint of their Ed25519 key. Subsequent
 * authenticated endpoints look up this account record to verify
 * per-request multisigs.
 *
 * **Authorization**: dual-stack multisig over the canonical message
 * `CREATE:{ed25519_pk}:{ml_dsa_pk}:{nonce}` using the very keys being
 * registered. The `X-Public-Key` header MUST equal the fingerprint
 * derived from `ed25519_pk`; otherwise the request is rejected as
 * malformed.
 */
export const createAccount = <ThrowOnError extends boolean = false>(
  options: Options<CreateAccountData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    CreateAccountResponses,
    CreateAccountErrors,
    ThrowOnError
  >({
    url: "/accounts",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });

/**
 * Verify a presented capability token.
 */
export const verifyCapability = <ThrowOnError extends boolean = false>(
  options: Options<VerifyCapabilityData, ThrowOnError>,
) =>
  (options.client ?? client).post<
    VerifyCapabilityResponses,
    VerifyCapabilityErrors,
    ThrowOnError
  >({
    url: "/capabilities/verify",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });
